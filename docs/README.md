# Julia Bridge Documentation

Documentation lives in the main navegador repo's `docs/` directory. The only Julia-bridge-specific doc is `JULIA_BRIDGE_PRIMER.md` in this directory.

For the full project documentation, see: `../navegador/docs/`
